import { AppSearchPipe } from './app-search.pipe';

describe('AppSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AppSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
