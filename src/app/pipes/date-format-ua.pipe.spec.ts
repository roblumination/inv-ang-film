import { DateFormatUaPipe } from './date-format-ua.pipe';

describe('DateFormatUaPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatUaPipe();
    expect(pipe).toBeTruthy();
  });
});
